{
  "name": "online-web",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/online/web",
  "projectType": "application",
  "tags": ["type:app", "framework:next", "scope:online"],
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/online/web"
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "online-web:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "online-web:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "online-web:build:production",
          "dev": false
        }
      }
    },
    "dev": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "online-web:build",
        "dev": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "@nx/js:tsc",
      "options": {
        "tsConfig": "apps/online/web/tsconfig.json",
        "main": "apps/online/web/app/page.tsx",
        "outputPath": "dist/apps/online/web"
      }
    }
  }
}
